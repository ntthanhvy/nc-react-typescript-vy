import styled from 'styled-components'
import { Cell } from 'styled-css-grid'

export const StyledCartItems = styled(Cell)`
  width: 90%;
  max-height: 90px;

  & span {
    margin: 20px;
  }
`

export const CartProduct = styled.div``
