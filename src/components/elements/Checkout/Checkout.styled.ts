import styled from 'styled-components'
import { Grid } from 'styled-css-grid'

export const StyledCheckout = styled(Grid)``

export const StyledCheckoutTitle = styled.span``

export const StyledCheckoutBody = styled(Grid)``

export const StyledCheckoutBtn = styled.button``